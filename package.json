{
  "name": "limited-request-queue",
  "description": "Interactively manage concurrency for outbound requests.",
  "version": "4.0.0-alpha",
  "license": "MIT",
  "author": "Steven Vachon <contact@svachon.com> (https://www.svachon.com/)",
  "main": "lib",
  "browser": {
    "parse-domain": "./lib/parseDomain"
  },
  "repository": "stevenvachon/limited-request-queue",
  "dependencies": {
    "defined": "^1.0.0",
    "isurl": "^1.0.0",
    "parse-domain": "^1.1.0"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1",
    "babelify": "^7.3.0",
    "browserify": "^14.4.0",
    "chai": "^4.0.2",
    "coveralls": "^2.13.1",
    "gzip-size-cli": "^2.0.0",
    "mocha": "^4.0.0",
    "nyc": "^11.0.3",
    "uglify-js": "^3.0.23",
    "universal-url": "^1.0.0"
  },
  "engines": {
    "node": ">= 6"
  },
  "scripts": {
    "ci": "npm run test && nyc report --reporter=text-lcov | coveralls",
    "posttest": "nyc report --reporter=html && browserify lib/ --global-transform [ babelify --presets [ es2015 ] ] --standalone RequestQueue | uglifyjs --compress --mangle | gzip-size",
    "test": "nyc --reporter=text-summary mocha test/ --check-leaks --bail"
  },
  "files": [
    "lib"
  ],
  "keywords": [
    "background",
    "concurrency",
    "DoS",
    "http",
    "limiting",
    "queue",
    "rate",
    "request",
    "throttle"
  ]
}
